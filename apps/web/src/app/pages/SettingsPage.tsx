export default function SettingsPage() {
  return (
    <section className="nx-stack nx-auth-card">
      <h2>Settings</h2>
      <p className="nx-muted">Account, appearance and integration settings arrive in later phases.</p>
    </section>
  );
}
